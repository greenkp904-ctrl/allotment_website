services:
  - type: web
    name: allotment
    env: docker
    buildCommand: docker build -t allotment .
    startCommand: docker run -p $PORT:8080 allotment
    envVars:
      - key: JAVA_VERSION
        value: "17"
